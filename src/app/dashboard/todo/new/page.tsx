import { PostCreateView } from 'src/sections/todo/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new todo',
};

export default function PostCreatePage() {
  return <PostCreateView />;
}
